# Archived Scaffold

This directory is an **unused Flutter template** (`your_new_app_name`). It is not the Apex Scheduler app.

**Active brewpub scheduling app:** `C:\development\projects\apex\apex`

Do not develop here unless intentionally restarting a separate Jigsy project.
